-기본 구조
코드작성 -> 코드 컴파일 -> JVM으로 실행

-cmd에서 실행
javac 파일명.java -> 파일명.class가 만들어진다.
java 파일명 

-형변환
큰거에 작은걸 넣는건 된다. 묵시적형변환
작은거에 큰걸 넣으려면 강제로 표현해줘야한다.
int x = 5
long y = x; //됨

int z = y; // 안됨. 되게하려면 타입을 써줘야한다.
int z = (int) y; // 됨